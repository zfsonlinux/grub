diff -Nur -x '*.orig' -x '*~' grub2-1.96+20080512/util/grub.d/10_linux.in grub2-1.96+20080512.new/util/grub.d/10_linux.in
--- grub2-1.96+20080512/util/grub.d/10_linux.in	2008-04-30 06:06:27.000000000 -0500
+++ grub2-1.96+20080512.new/util/grub.d/10_linux.in	2008-05-28 09:33:56.000000000 -0500
@@ -24,7 +24,7 @@
 if [ "x${GRUB_DISTRIBUTOR}" = "x" ] ; then
   OS=GNU/Linux
 else
-  OS="${GRUB_DISTRIBUTOR} GNU/Linux"
+  OS="${GRUB_DISTRIBUTOR}"
 fi
 
 test_numeric ()
@@ -118,7 +118,7 @@
 
   cat << EOF
 menuentry "${OS}, linux ${version} (single-user mode)" {
-	linux	${grub_dirname}/${basename} root=${GRUB_DEVICE} ro single ${GRUB_CMDLINE_LINUX}
+	linux	${grub_dirname}/${basename} root=${GRUB_DEVICE} ro single
 EOF
   if test -n "${initrd}" ; then
     cat << EOF
